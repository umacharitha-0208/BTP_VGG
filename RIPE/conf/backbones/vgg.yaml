_target_: ripe.models.backbones.vgg.VGG
nchannels: 3
pretrained: True
use_instance_norm: False
mode: dect
use_gradient_checkpointing: True  # recompute activations during backward — critical for full VGG on GPU
freeze_layers: 10                 # freeze first 10 layers — same as original RIPE training
